libraries:
sklearn
numpy

savedata.py will generate csv's of the test/train split (run before the other .py's)

Each .py performs several fits and reports the accuracy thereof.

knn.py : k-Nearest Neighbor Classifier
svm.py : SVM Classifier
mlp.py : MLP Classifier